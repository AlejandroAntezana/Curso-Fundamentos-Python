'''
Doble de cada número:
Dada una lista de números, utiliza map() y una función lambda para obtener una nueva lista que contenga
el doble de cada número.

Cuadrado de cada número:
Similar al ejercicio anterior, pero esta vez obtén una lista que contenga el cuadrado de cada número.

Convertir a mayúsculas:
Dada una lista de palabras, utiliza map() y una función lambda para obtener una nueva lista con todas
las palabras en mayúsculas.

Longitud de cada palabra:
Dada una lista de palabras, utiliza map() y una función lambda para obtener una nueva lista con la longitud
de cada palabra.

Calificaciones a letras:
Dada una lista de calificaciones (números), utiliza map() y una función lambda para convertir cada calificación
a una letra según la siguiente escala: 90-100: A, 80-89: B, 70-79: C, 60-69: D, por debajo de 60: F.

Reemplazar vocales:
Dada una lista de palabras, utiliza map() y una función lambda para obtener una nueva lista en la que cada vocal
sea reemplazada por un guion bajo (_). Por ejemplo, "python" se convierte en "pyth_n".

Multiplicación de elementos en listas:
Dadas dos listas de números del mismo tamaño, utiliza map() y una función lambda para obtener una nueva lista que
contenga la multiplicación elemento por elemento de las dos listas.

Suma de dígitos:
Dada una lista de números de varios dígitos, utiliza map() y una función lambda para obtener una nueva lista que
contenga la suma de los dígitos de cada número. Por ejemplo, para el número 123, la suma sería 1 + 2 + 3 = 6.

Cambio de moneda:
Dada una lista de cantidades en dólares, utiliza map() y una función lambda para convertir cada cantidad a euros.
Utiliza un factor de cambio hipotético.

'''

